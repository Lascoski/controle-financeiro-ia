apiVersion: v1
kind: ConfigMap

metadata:
  name: app-config

data:
  FLASK_ENV: "production"
  POSTGRES_DB: "financas"
  POSTGRES_USER: "postgres"
  POSTGRES_HOST: "postgres-service"
  POSTGRES_PORT: "5432"